{
    "aws_region": "{{env `AWS_REGION`}}",
    "aws_access_key": "{{env `AWS_ACCESS_KEY_ID`}}",
    "aws_secret_key": "{{env `AWS_SECRET_ACCESS_KEY`}}",
    "source_ami": "ami-03d315ad33b9d49c4",
    "subnet_id": "{{env `AWS_SUBNET_ID`}}",
    "prod_account_id": "{{env `AWS_PROD_ACCOUNT_ID`}}",
    "ssh_username": "ubuntu"
}